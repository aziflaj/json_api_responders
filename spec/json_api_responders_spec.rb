require 'spec_helper'

describe JsonApiResponders do
  it 'has a version number' do
    expect(JsonApiResponders::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
